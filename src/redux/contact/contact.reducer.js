import ContactActionTypes from './contact.types'

const INITIAL_STATE ={
  error: null
}

const contactReducer = (state = INITIAL_STATE, action)={
  switch (action.type) {
    case ContactActionTypes.
      
      break;
  
    default:
      break;
  }
}
